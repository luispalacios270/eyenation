;( function( video911App ) {

    "use strict";

    video911App.factory( "securityDelegateFactory", function( $http ) {

            var delegate = {};


            return delegate;
    } );

} )( video911App );
